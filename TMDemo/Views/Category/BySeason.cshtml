@model IEnumerable<TMDemo.Models.GroupedTreksByCategory>

<div class="container">
    <h2>Treks Categorized by Season</h2>

    @if (Model.Any())
    {
        foreach (var group in Model)
        {
            <div class="season-group">
                
                <h3 class="season-title">@group.Category</h3>

                <div class="row">
                    @foreach (var trek in group.Treks)
                    {
                        <!-- Use the existing partial view to display each trek -->
                        @await Html.PartialAsync("_TrekCard", trek)
                    }
                </div>
            </div>
        }
    }
    else
    {
        <p>No treks available for any season.</p>
    }
</div>

<style>
    .season-group {
        margin-bottom: 40px;
    }

    .season-title {
        font-size: 1.5rem;
        color: #007bff;
        margin-bottom: 20px;
    }
</style>
