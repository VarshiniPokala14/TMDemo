@model TrekMasters.Models.TrekReview

<div class="review mt-4 bg-white p-4 rounded-lg shadow">
    <p class="font-bold">@Model.User?.UserName</p>
    <p class="text-sm text-gray-500">@Model.CreatedAt.ToString("MMMM dd, yyyy")</p>
    <p class="mt-2">@Model.Comment</p>
</div>
